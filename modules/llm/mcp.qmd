---
title: "🔌 MCP"
---

Intégrer un LLM dans une application d'entreprise, c'est comme changer de pays avec ses appareils électroniques : chaque connecteur est différent. Le **Model Context Protocol (MCP)** est la réponse de l'industrie — le port USB-C universel de l'IA.

## 🧩 Le Problème N×M

Sans standard, connecter **N** fournisseurs de LLM (OpenAI, Anthropic, Google…) à **M** applications d'entreprise (Slack, GitHub, bases de données…) exige **N × M** intégrations sur mesure. Chaque connexion parle un dialecte différent, se brise à chaque mise à jour d'API, et génère une dette technique incontrôlable.

::: {.row gap=4}
:::: {.col}
::::: {.card}
#### Sans MCP : L'Explosion Combinatoire {.bi-diagram-2}
**3 LLM × 5 outils = 15 connecteurs custom** à maintenir. Multiplié par les versions d'API et les fournisseurs… c'est une avalanche de code spaghetti.
:::::
::::
:::: {.col}
::::: {.card}
#### Avec MCP : Un Langage Commun {.bi-broadcast}
Un seul protocole universel. Chaque outil expose un **Serveur MCP** ; chaque LLM consomme un **Client MCP**. Quel que soit le fournisseur, la "prise" est désormais identique.
:::::
::::
:::

## 🏗️ L'Architecture

MCP définit trois acteurs qui communiquent via **JSON-RPC 2.0** — un protocole léger et standardisé pour appeler des fonctions à distance.

::: {.row gap=4}
:::: {.col}
::::: {.card}
#### Le Serveur MCP {.bi-hdd-network}
Programme **léger** qui expose données ou outils de manière standardisée.

*Exemples : lecture de PostgreSQL, appels GitHub, consultation de calendrier.*
:::::
::::
:::: {.col}
::::: {.card}
#### Le Client MCP {.bi-plug}
Le **connecteur** intégré dans votre framework IA (LangChain, LlamaIndex…). Il interroge le serveur, filtre les données sensibles, et relaie le résultat au LLM.
:::::
::::
:::: {.col}
::::: {.card}
#### L'Hôte {.bi-person-circle}
L'**application finale** avec laquelle l'utilisateur interagit : Claude Desktop, VS Code, un chatbot d'entreprise. C'est lui qui pilote le Client.
:::::
::::
:::

::: {.callout-tip}
La bibliothèque communautaire [Glama MCP Servers](https://glama.ai/mcp/servers) répertorie des centaines de serveurs prêts à l'emploi — bases de données, GitHub, Slack, outils de développement.
:::

::: {.callout-note collapse="true"}
#### Qu'est-ce que JSON-RPC 2.0 ?
JSON-RPC est un protocole d'appel de procédure à distance (*Remote Procedure Call*). Léger, lisible, indépendant du transport (HTTP, WebSocket, stdio) :
```json
{ "method": "tools/call", "params": { "name": "get_weather", "arguments": { "city": "Paris" } }, "id": 1 }
```
:::

## 🧠 La Boucle ReAct

Pour qu'un LLM utilise des outils sans se perdre, on lui impose un cadre de raisonnement : **ReAct (Reasoning + Acting)**. Contrairement à la simple "Chain-of-Thought", ReAct force l'agent à *alterner* entre réflexion et réalité — après chaque action, il *observe* le résultat avant de décider de la suite.

::: {.card-window}
#### Simulateur ReAct {.bi-arrow-repeat}

:::: {.tabs .react-tabset}
##### Boucle {.bi-diagram-3}
::::: {#react-loop .chart-container-md}
:::::

##### [Animer]{#btn-react .bi-play-fill .btn} {.tab-right .no-pane}
::::

:::

::: {.row gap=3 mt=3}
:::: {.col}
**💭 Pensée**

*"Je dois trouver la météo à Paris. Je vais utiliser `weather_api`."*
::::
:::: {.col}
**⚡ Action**

L'agent appelle `get_weather("Paris")` via le Client MCP.
::::
:::: {.col}
**👁️ Observation**

Le serveur renvoie `{"temp": 15, "condition": "Pluie"}`.
::::
:::: {.col}
**💭 Pensée**

*"Il pleut à Paris. Je peux formuler ma réponse finale."*
::::
:::

```{ojs}
//| echo: false
reactSim = {
  const nodes = [
    { id: "T", label: "Pensée", status: "entry", shape: "rounded rect" },
    { id: "A", label: "Action", status: "default", shape: "rounded rect" },
    { id: "O", label: "Observation", status: "default", shape: "rounded rect" }
  ];
  const links = [
    { source: "T", target: "A", label: "choisit un outil" },
    { source: "A", target: "O", label: "appel MCP" },
    { source: "O", target: "T", label: "apprend & ajuste" }
  ];
  const graph = renderStateMachineGraph("#react-loop", { nodes, links }, {
    height: 280,
    nodeRadius: 28,
    fontSize: 11,
    enableZoom: false,
    zoomToFit: true,
    zoomToFitPadding: 60
  });
  const steps = [
    { activeNode: "T", activeLink: null, pastNodes: [], pastLinks: [] },
    { activeNode: "A", activeLink: "T-A", pastNodes: ["T"], pastLinks: [] },
    { activeNode: "O", activeLink: "A-O", pastNodes: ["T", "A"], pastLinks: ["T-A"] },
    { activeNode: "T", activeLink: "O-T", pastNodes: ["T", "A", "O"], pastLinks: ["T-A", "A-O"] }
  ];
  const sm = new StateMachine({
    states: steps,
    interval: 1400,
    loop: true,
    onStateChange: (step) => {
      nodes.forEach(n => {
        n.status = n.id === step.activeNode ? "current" : step.pastNodes.includes(n.id) ? "past" : "default";
      });
      links.forEach(l => {
        const srcId = typeof l.source === "object" ? l.source.id : l.source;
        const tgtId = typeof l.target === "object" ? l.target.id : l.target;
        const lid = `${srcId}-${tgtId}`;
        l.status = lid === step.activeLink ? "current" : step.pastLinks.includes(lid) ? "past" : "default";
      });
      graph.graphData({ nodes, links });
    }
  });
  invalidation.then(() => { sm.stop(); graph?.destroy?.(); });
  return { graph, sm };
}
```

```{ojs}
//| echo: false
_toggleReact = {
  if (btn_react === 0) return;
  const { sm } = reactSim;
  if (sm.isPlaying) { sm.stop(); sm.currentIndex = 0; }
  else sm.start();
}
```

## ✂️ Divulgation Progressive

Un agent avec accès à 1 000 outils ne peut pas charger *tous* leurs schémas en contexte : il saturerait la fenêtre du LLM avant même de répondre.

La **Divulgation Progressive** (*Progressive Disclosure*) résout ce problème : l'agent explore l'arborescence des outils **à la demande**, comme un explorateur de fichiers. Il ne charge que le schéma de l'outil dont il a besoin *maintenant*.

::: {.card-window}
#### Schémas d'outils chargés en contexte {.bi-bar-chart-line}

:::: {#plot-tokens .chart-container-sm}
::::

::: {.callout-tip}
En pratique, un agent avec 1 000 outils disponibles n'en charge en moyenne que **~13 par tâche** — soit une réduction de **98,7 % des tokens** consommés par les définitions d'outils.
:::

:::

```{ojs}
//| echo: false
renderTokens = {
  createBar("plot-tokens", {
    x: ["Chargement naïf\n(tout charger)", "Divulgation Progressive\n(à la demande)"],
    y: [1000, 13]
  }, "Schémas d'outils chargés en contexte", {
    marker: {
      color: [
        getThemeColor("--sol-red", "#dc322f"),
        getThemeColor("--sol-green", "#859900")
      ]
    },
    layout: {
      margin: { t: 45, b: 70, l: 50, r: 20 },
      paper_bgcolor: "transparent",
      plot_bgcolor: "transparent"
    }
  })
}
```

## 🔒 Sécurité Intégrée

Le Client MCP ne se contente pas de relayer des données : il agit comme un **pare-feu actif** entre les systèmes d'entreprise et le LLM hébergé dans le Cloud.

::: {.row gap=4}
:::: {.col}
::::: {.card}
#### Interception des PII {.bi-shield-check}
Avant d'envoyer les données au LLM, le Client peut **tokeniser** les informations personnelles (numéros de sécurité sociale, IBAN, adresses). Le LLM reçoit un jeton opaque `PII_2847` au lieu de la vraie valeur — les données sensibles ne quittent jamais le périmètre de confiance de l'entreprise.
:::::
::::
:::: {.col}
::::: {.card}
#### Conformité sans Friction {.bi-check2-square}
L'architecture garantit la **conformité RGPD** sans modifier les systèmes métier existants. C'est le principe du **Zero-Trust by Design** : le modèle tiers n'a jamais accès aux données brutes.
:::::
::::
:::

## 🐝 Multi-Agents

Pour les tâches trop complexes pour un seul agent, on recourt à une **architecture Leader-Worker** : un agent "chef d'orchestre" décompose la requête et délègue à des agents spécialisés, qui communiquent entre eux via le protocole **A2A (Agent-to-Agent)**.

::: {.card-window}
#### Architecture Leader-Worker {.bi-diagram-3}

:::: {#multi-agent-diagram .chart-container-md}
::::

:::

```{ojs}
//| echo: false
multiAgentGraph = {
  const nodes = [
    { id: "U", label: "Utilisateur", shape: "circle", status: "entry" },
    { id: "L", label: "Agent Leader", shape: "rounded rect", status: "default" },
    { id: "C", label: "Agent Chercheur", shape: "pill", status: "default" },
    { id: "A", label: "Agent Analyste", shape: "pill", status: "default" },
    { id: "WS", label: "Recherche Web", shape: "oval", status: "past" },
    { id: "DB", label: "Base de données", shape: "oval", status: "past" }
  ];
  const links = [
    { source: "U", target: "L", label: "requête" },
    { source: "L", target: "C", label: "délègue" },
    { source: "L", target: "A", label: "délègue" },
    { source: "C", target: "WS", label: "MCP" },
    { source: "A", target: "DB", label: "MCP" },
    { source: "L", target: "U", label: "synthèse" }
  ];
  const graph = renderStateMachineGraph("#multi-agent-diagram", { nodes, links }, {
    height: 320,
    nodeRadius: 22,
    fontSize: 9,
    enableZoom: false,
    zoomToFit: true,
    zoomToFitPadding: 40
  });
  invalidation.then(() => graph?.destroy?.());
  return graph;
}
```

::: {.callout-note collapse="true"}
#### Pourquoi plusieurs agents plutôt qu'un seul ?
Un LLM unique sur une tâche très longue accumule les erreurs : plus le contexte grandit, plus les hallucinations se multiplient. Découper en agents spécialisés avec des contextes courts et ciblés réduit drastiquement ce phénomène. Chaque agent ne fait qu'une chose, mais la fait bien.
:::
